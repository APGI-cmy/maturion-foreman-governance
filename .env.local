# ======================================================
# SAFE LOCAL ENVIRONMENT (NO REAL SECRETS)
# Purpose: Allow Foreman UI to run locally without risk
# ======================================================

# === GitHub / MCP (INTENTIONALLY DISABLED LOCALLY) ===
GITHUB_MCP_TOKEN=LOCAL_ONLY_DISABLED
GITHUB_APP_ID=LOCAL_ONLY_DISABLED
GITHUB_APP_PRIVATE_KEY=LOCAL_ONLY_DISABLED
GITHUB_APP_INSTALLATION_ID=LOCAL_ONLY_DISABLED

# === MCP Access (Foreman → MCP) ===
# Foreman will NOT execute mutations locally
MCP_API_KEY=LOCAL_ONLY_DISABLED

# === Foreman App Base URL ===
NEXT_PUBLIC_APP_URL=http://localhost:3000

# === OpenAI / Model Escalation (SAFE PLACEHOLDER) ===
OPENAI_API_KEY=LOCAL_ONLY_DISABLED
FOREMAN_DEFAULT_MODEL=gpt-4o-mini
FOREMAN_MAX_MODEL=gpt-5.1

# === Autonomous Execution Flags ===
# Local autonomy is READ-ONLY
FOREMAN_AUTONOMY_ENABLED=false
MATURION_AUTO_MERGE_ENABLED=false
MATURION_AUTO_MERGE_METHOD=squash

# === Logging & Debug ===
FOREMAN_DEBUG=true

# === Builder Network (LOCAL ONLY / SIMULATION) ===
LOCAL_BUILDER_URL=http://localhost:8000
COPILOT_BUILDER_ENABLED=true
LOCAL_BUILDER_ENABLED=true

# === UI Flags (SAFE TO EXPOSE) ===
NEXT_PUBLIC_FOREMAN_AUTONOMY_UI_FLAG=true

# This workflow manages governance checks

name: Governance Gate

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  governance:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Lint
      run: | 
        for dir in app components lib types tests foreman swarm; do
          # Lint the directory
          echo "Linting directory: $dir"
        done

    - name: Test
      run: |
        for dir in app components lib types tests foreman swarm; do
          # Test the directory
          echo "Testing directory: $dir"
        done

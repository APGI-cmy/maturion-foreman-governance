{
  "name": "agent-bootstrap",
  "version": "1.0.0",
  "description": "MCP server providing the agent_bootstrap tool — enforces constitutional preflight for all governed agents",
  "main": "index.js",
  "scripts": {
    "prestart": "npm ci --prefer-offline 2>/dev/null || npm install",
    "start": "node index.js",
    "test": "node test-bootstrap.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "zod": "^3.22.4"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "UNLICENSED"
}

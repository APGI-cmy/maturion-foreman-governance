# PR #1375 — QA-to-Red Evidence

**Evidence ID**: PR1375-QA-TO-RED-20260727  
**Recorded**: 2026-07-27T13:20:24Z  
**Repository**: APGI-cmy/maturion-foreman-governance  
**Issue**: #1374  
**PR**: #1375  
**Preflight parent**: administrator preflight checkpoint after `ac399dba…`  
**Agent**: governance-repo-administrator-v2  
**Outcome**: RED AS REQUIRED  
**Implementation status**: NOT STARTED

## Fixtures

- `scripts/tests/test_regenerate_canon_inventory.py`
- `.github/scripts/tests/test-canon-inventory-provenance.sh`

The fixtures are executable and use isolated temporary Git repositories. They do not modify the
canonical inventory, canonical documents, producer scripts, workflows, consumers, deployments,
or live environments.

## Generator RED

Command:

```text
python3 -m unittest -v scripts.tests.test_regenerate_canon_inventory
```

Result: exit `1`; four tests executed; one assertion failure and three expected contract errors.

| Fixture | Current failure |
|---|---|
| reconstruct each entry from its content-producing commit | generated entry has no `canonical_commit` |
| preserve independently verifiable existing provenance | generated entry has no `canonical_commit` |
| replace synthetic shared HEAD with content-producing commit | generated entry has no `canonical_commit` |
| byte-identical generation for a fixed repository state | `generation_timestamp` changes between runs |

## Validator RED

Command:

```text
bash .github/scripts/tests/test-canon-inventory-provenance.sh
```

Result: exit `1`.

The positive control passed: a fixture with valid path/hash/content-producing commits was
accepted.

The current validator incorrectly accepted all six negative boundaries:

1. missing canonical commit provenance;
2. malformed canonical commit provenance;
3. unknown/non-resolving commit;
4. commit where the declared path does not exist;
5. commit whose path bytes do not match the declared hash;
6. synthetic common `HEAD` reuse for a file not produced by that commit.

The test harness therefore reported:

```text
QA_TO_RED: 6 provenance boundary fixture(s) are not enforced
PYTHON_RED_EXIT=1
SHELL_RED_EXIT=1
```

## Scope and Ordering Assertion

This RED evidence follows the committed task set, independent IAA pre-brief, bounded appointment,
and administrator frozen-contract preflight. At this checkpoint:

- QA exists before implementation;
- the current generator and validator remain unchanged;
- `governance/CANON_INVENTORY.json` remains unchanged with 203/203 missing provenance;
- no semantic canon or policy byte changed;
- no task was ticked;
- QP, ECAP, PREHANDOVER, final IAA, merge, layer-down, and ISMS resumption remain pending.

## Build-to-Green Contract

Later implementation must make these unchanged fixtures Green by:

- resolving the content-producing commit per path/hash;
- preserving an existing value only when independently verifiable;
- producing byte-identical output for a fixed repository state;
- rejecting every fail-closed negative boundary;
- rejecting synthetic shared-HEAD assignment when the commit did not produce the file bytes.

---

This immutable artifact records QA-to-Red only. It is not implementation evidence, QP, ECAP,
PREHANDOVER, final IAA, merge-readiness, or consumer layer-down authority.
